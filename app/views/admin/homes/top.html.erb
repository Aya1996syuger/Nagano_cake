<h4>
 <span class="title">注文履歴一覧</span><br>
</h4>
<div class="order_details">
  <table class="table table-hover table-inverse">
    <thead>
    <tr>
      <th>購入日時</th>
      <th>購入者</th>
    </tr>
   </thead>


  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.created_at.strftime('%Y/%m/%d') %></td>
        <td><%= order.name %></td>
        
    
       
     
    <% end %>
    </tr>
  </tbody>
</table>